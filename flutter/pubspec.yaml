name: nutrition_scanner
description: Nutrition label scanning — manual and stability-gated auto capture.
version: 0.1.0
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  camera: ^0.11.0
